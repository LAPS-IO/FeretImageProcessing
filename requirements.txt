# Python 3.12 (developed/tested on 3.12.2)
# Core image/array deps
numpy>=1.26,<2
opencv-python>=4.11

# Optional: progress bars in feret.py / extract_rois.py (falls back to no-op if missing)
tqdm>=4.66
